# Frontend

React + Vite single-page application for the writing and translation workspace.

## Local Start

```bash
npm install
npm run dev
```

The app will be available at `http://localhost:5173`.

