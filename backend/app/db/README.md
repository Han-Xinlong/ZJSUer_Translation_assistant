# Data Layer

This directory is reserved for persistence code.

Planned storage strategy:

- Frontend-first local records with IndexedDB.
- Optional backend SQLite storage for local development and future sync.
- Cloud backup only after privacy and consent flows are designed.

